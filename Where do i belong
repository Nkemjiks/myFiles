function getIndexToIns(arr, num) {
  arr.push(num);
  console.log(arr);
  arr.sort(function (a,b) {
  	return a - b;
  });
  console.log(arr);
  var now = arr.indexOf(num);
  console.log(now);
}

getIndexToIns([40, 60], 50);