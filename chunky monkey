function chunkArrayInGroups(arr, size) {

	var arrc = [];
	for (var i = 0; i < arr.length; i += size) {
			arrc.push(arr.slice(i, i + size));
}
	console.log(arrc);
}

chunkArrayInGroups([0, 1, 2, 3, 4, 5], 4);

